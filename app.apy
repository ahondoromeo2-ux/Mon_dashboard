from flask import Flask, render_template, request
import smtplib
from email.mime.text import MIMEText

@app.route("/envoyer_email", methods=["POST"])
def envoyer_email():
    contenu = request.form.get("contenu")
    destinataire = request.form.get("destinataire")

    msg = MIMEText(contenu)
    msg["Subject"] = "Résultat Laboratoire IA"
    msg["From"] = "ton_adresse@gmail.com"
    msg["To"] = destinataire

    # ⚠️ Exemple avec Gmail (nécessite mot de passe d’application)
    with smtplib.SMTP_SSL("smtp.gmail.com", 465) as serveur:
        serveur.login("ton_adresse@gmail.com", "TON_MOT_DE_PASSE_APP")
        serveur.send_message(msg)

    return "Email envoyé avec succès ✅"
